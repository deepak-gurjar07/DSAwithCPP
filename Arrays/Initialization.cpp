#include <iostream>
using namespace std;

int main(){


    // intialization, input and ouput of 1D array
/*
    int size;
    cout<<"Enter the size of array: ";
    cin>>size;
    int  arr[size] ;
    cout<<"Enter the elements of array:"<<endl;

    for (int i = 0; i < size; i++)
    {
        cin>>arr[i];
    }

    cout<<"The elements of array:"<<endl;
    for (int i = 0; i < size; i++)
    {
        cout<<arr[i]<<" ";
    }
    */
    
     // intialization, input and ouput of 2D array

    

    return 0;
}